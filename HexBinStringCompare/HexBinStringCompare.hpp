/* COPYRIGHT
 * Given the binary and hexadecimal representation
 * as two strings, return true if two representations are
 * equal, otherwise return false.
*/

#ifndef HEXBINSTRINGCOMPARE_HEXBINSTRINGCOMPARE_HPP_
#define HEXBINSTRINGCOMPARE_HEXBINSTRINGCOMPARE_HPP_

#include <string>

bool HexBinCompare(std::string const& bin_str, std::string const& hex_str);

#endif  // HEXBINSTRINGCOMPARE_HEXBINSTRINGCOMPARE_HPP_
